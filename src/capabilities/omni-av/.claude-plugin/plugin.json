{
  "name": "qwen-mm-plugins-omni-av",
  "version": "1.0.0",
  "description": "Qwen-MM-Plugins omni-av — Omni-model audio/video tools: segmented captioning with timestamps, ASR (plain / controllable / multi-speaker diarized), temporal grounding, and event counting, exposed as an MCP server.",
  "skills": ["./skill"],
  "mcpServers": {
    "qwen-mm-plugins-omni-av": {
      "command": "uvx",
      "args": ["--from", "qwen-mm-plugins[omni-av] @ git+https://github.com/QwenLM/Qwen-MM-Plugins.git@main", "qwen-mm-plugins-omni-av"]
    }
  }
}
